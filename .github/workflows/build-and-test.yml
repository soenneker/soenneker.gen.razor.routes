name: build-and-test

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  PipelineEnvironment: true
  NUGET_XMLDOC_MODE: skip

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore build tasks
        run: dotnet restore src/Soenneker.Gen.Razor.Routes.BuildTasks/Soenneker.Gen.Razor.Routes.BuildTasks.csproj --verbosity minimal

      - name: Build build tasks
        run: dotnet build src/Soenneker.Gen.Razor.Routes.BuildTasks/Soenneker.Gen.Razor.Routes.BuildTasks.csproj --configuration Release --no-restore --verbosity minimal

      - name: Restore tests
        run: dotnet restore test/Soenneker.Gen.Razor.Routes.Tests/Soenneker.Gen.Razor.Routes.Tests.csproj --verbosity minimal

      - name: Build tests
        run: dotnet build test/Soenneker.Gen.Razor.Routes.Tests/Soenneker.Gen.Razor.Routes.Tests.csproj --configuration Release --no-restore --verbosity minimal

      - name: Test
        run: dotnet test --project test/Soenneker.Gen.Razor.Routes.Tests/Soenneker.Gen.Razor.Routes.Tests.csproj --configuration Release --no-build --no-restore --verbosity minimal

      - name: Restore library
        run: dotnet restore src/Soenneker.Gen.Razor.Routes/Soenneker.Gen.Razor.Routes.csproj --verbosity minimal

      - name: Build library
        run: dotnet build src/Soenneker.Gen.Razor.Routes/Soenneker.Gen.Razor.Routes.csproj --configuration Release --no-restore --verbosity minimal

      - name: Pack
        run: dotnet pack src/Soenneker.Gen.Razor.Routes/Soenneker.Gen.Razor.Routes.csproj --configuration Release --no-build --no-restore --output . --verbosity minimal
